# 🔧 Исправление ошибки доступа к базе данных

## Проблема

```
SQLSTATE[HY000] [1045] Access denied for user 'nikolr4t'@'localhost' (using password: YES)
```

Это означает, что:
- Пользователь `nikolr4t` существует, но пароль неправильный
- ИЛИ username отличается от `nikolr4t`

## Решение

### Вариант 1: Проверьте правильный username

На Beget username для БД обычно совпадает с префиксом имени БД, но может отличаться.

**Как проверить:**
1. Войдите в панель управления Beget
2. Перейдите в раздел "Базы данных MySQL"
3. Найдите вашу БД `nikolr4t_portfol`
4. Посмотрите **имя пользователя** - оно может быть:
   - `nikolr4t` (если совпадает с префиксом)
   - `nikolr4t_portfol` (полное имя БД)
   - Другое имя (если вы создавали пользователя отдельно)

### Вариант 2: Обновите пароль в workflow

Если username правильный, но пароль неправильный:

1. Откройте `.github/workflows/deploy.yml`
2. Найдите строку с паролем (строка 99):
   ```yaml
   'password' => '4I3%8gC2QF86',
   ```
3. Замените на правильный пароль от БД

### Вариант 3: Создайте config.php вручную на сервере

Если автоматическое создание не работает:

1. Подключитесь к серверу через FTP
2. Создайте файл `/public_html/database/config.php`:
   ```php
   <?php
   return [
       'host' => 'localhost',
       'dbname' => 'nikolr4t_portfol',
       'username' => 'ПРАВИЛЬНЫЙ_USERNAME', // Замените на правильный
       'password' => 'ПРАВИЛЬНЫЙ_ПАРОЛЬ',   // Замените на правильный
       'charset' => 'utf8mb4',
   ];
   ```

### Вариант 4: Используйте GitHub Secret для пароля БД

Более безопасный способ - хранить пароль БД в секретах:

1. Добавьте секрет `FTP_DB_PASSWORD` в GitHub
2. Обновите workflow для использования секрета

## Быстрая проверка

1. **Проверьте username:**
   - В панели Beget → Базы данных MySQL
   - Посмотрите имя пользователя для БД `nikolr4t_portfol`

2. **Проверьте пароль:**
   - Убедитесь, что используете правильный пароль
   - Пароль: `4I3%8gC2QF86` (если вы его не меняли)

3. **Проверьте права доступа:**
   - Убедитесь, что пользователь имеет права на БД `nikolr4t_portfol`

## После исправления

После обновления config.php:
1. Проверьте: `https://develonik.ru/api/test-db.php`
2. Должно быть: `{"success":true,"message":"Database connected successfully!"}`

