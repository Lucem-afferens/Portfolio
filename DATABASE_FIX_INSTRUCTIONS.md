# 🚨 СРОЧНО: Исправление ошибки доступа к БД

## Текущая ошибка

```
Access denied for user 'nikolr4t'@'localhost' (using password: YES)
```

## Быстрое решение (выберите один вариант)

### ✅ Вариант 1: Проверьте username в панели Beget (РЕКОМЕНДУЕТСЯ)

1. Войдите в панель управления Beget
2. Перейдите: **Базы данных MySQL** → найдите `nikolr4t_portfol`
3. Посмотрите **имя пользователя** - оно может быть:
   - `nikolr4t` (если совпадает)
   - `nikolr4t_portfol` (полное имя)
   - Другое (если создавали отдельно)

4. **Если username отличается:**
   - Добавьте секрет `FTP_DB_USERNAME` в GitHub (Settings → Secrets)
   - Укажите правильный username

### ✅ Вариант 2: Создайте config.php вручную на сервере

Если нужно исправить СЕЙЧАС без деплоя:

1. Подключитесь к серверу через FTP
2. Откройте файл: `/public_html/database/config.php`
3. Проверьте/исправьте:
   ```php
   <?php
   return [
       'host' => 'localhost',
       'dbname' => 'nikolr4t_portfol',
       'username' => 'ПРАВИЛЬНЫЙ_USERNAME', // ← Проверьте это!
       'password' => '4I3%8gC2QF86',        // ← Проверьте это!
       'charset' => 'utf8mb4',
   ];
   ```

4. Сохраните файл
5. Проверьте: `https://develonik.ru/api/test-db.php`

### ✅ Вариант 3: Используйте GitHub Secrets

1. В GitHub: **Settings** → **Secrets and variables** → **Actions**
2. Добавьте секреты:
   - `FTP_DB_USERNAME` = правильный username (если отличается)
   - `FTP_DB_PASSWORD` = правильный пароль (если отличается)
3. Сделайте push в `main` - workflow обновит config автоматически

## Как узнать правильный username?

**Способ 1: Панель Beget**
- Базы данных MySQL → ваша БД → посмотрите "Пользователь"

**Способ 2: phpMyAdmin**
- Войдите в phpMyAdmin
- Вверху справа будет показан текущий пользователь

**Способ 3: Попробуйте варианты**
- `nikolr4t`
- `nikolr4t_portfol`
- Префикс вашего аккаунта

## После исправления

Проверьте:
```
https://develonik.ru/api/test-db.php
```

Должно быть:
```json
{
  "success": true,
  "message": "Database connected successfully!",
  "tables": "Testimonials table exists"
}
```

## Если ничего не помогает

1. Проверьте, что БД `nikolr4t_portfol` существует
2. Проверьте, что пользователь имеет права на эту БД
3. Создайте нового пользователя в панели Beget с правильными правами
4. Обновите config.php с новыми данными


