# 🔍 Проверка статуса workflow в GitHub

## Как проверить статус workflow

### 1. Проверьте вкладку Actions

1. Откройте ваш репозиторий на GitHub
2. Нажмите на вкладку **"Actions"** в верхнем меню
3. Вы должны увидеть список запусков workflow
4. Последний запуск должен быть виден с статусом:
   - ✅ Зеленая галочка = успешно
   - ❌ Красный крестик = ошибка
   - 🟡 Желтый кружок = выполняется

### 2. Проверьте статус на главной странице репозитория

На главной странице репозитория (вверху, рядом с последним коммитом) должен быть индикатор статуса:
- ✅ Зеленая галочка
- ❌ Красный крестик
- 🟡 Желтый кружок

### 3. Если статус не отображается

**Возможные причины:**

1. **Workflow отключен:**
   - Actions → найдите "Deploy Portfolio to Beget"
   - Убедитесь, что workflow включен
   - Если отключен - нажмите "Enable workflow"

2. **Workflow не запустился:**
   - Проверьте, что вы делали push в ветку `main`
   - Проверьте, что файл `.github/workflows/deploy.yml` существует
   - Проверьте, что последний коммит не был в `paths-ignore`

3. **Проверьте настройки репозитория:**
   - Settings → Actions → General
   - Убедитесь, что "Allow all actions and reusable workflows" включено

### 4. Запустить workflow вручную

1. Перейдите в **Actions**
2. Выберите workflow "Deploy Portfolio to Beget"
3. Нажмите **"Run workflow"** (справа вверху)
4. Выберите ветку `main`
5. Нажмите **"Run workflow"**

### 5. Проверка логов

Если workflow запустился, но статус непонятный:
1. Откройте конкретный запуск в Actions
2. Посмотрите логи каждого шага
3. Найдите ошибки (красные строки)

## Быстрая проверка

```bash
# Проверьте последний коммит
git log --oneline -1

# Проверьте, что workflow файл существует
ls -la .github/workflows/deploy.yml

# Проверьте статус в GitHub
# Откройте: https://github.com/ваш-username/Portfolio/actions
```

## Если ничего не помогает

1. **Проверьте права доступа:**
   - Убедитесь, что у вас есть права на запуск Actions
   - Проверьте настройки организации (если репозиторий в организации)

2. **Проверьте секреты:**
   - Settings → Secrets and variables → Actions
   - Убедитесь, что все необходимые секреты добавлены

3. **Создайте тестовый коммит:**
   ```bash
   echo "# Test" >> README.md
   git add README.md
   git commit -m "test: trigger workflow"
   git push
   ```

---

**После этих шагов workflow должен запуститься и статус появится!**


