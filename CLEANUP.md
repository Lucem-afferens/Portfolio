# 🧹 Очистка временных файлов

## Файлы, которые можно удалить на сервере

После успешной настройки и добавления проектов можно удалить следующие временные файлы:

### 1. Скрипты проверки (уже удалены из репозитория)

Эти файлы уже удалены из репозитория и не будут деплоиться:
- ✅ `api/check-permissions.php` - скрипт проверки прав доступа
- ✅ `api/admin/seed-projects.php` - старый скрипт добавления проектов

**На сервере:** Удалите вручную через панель управления или SSH:
```bash
rm /home/n/nikolr4t/develonik.ru/public_html/api/check-permissions.php
rm /home/n/nikolr4t/develonik.ru/public_html/api/admin/seed-projects.php
```

### 2. SQL скрипты (можно оставить как резервную копию)

Эти файлы можно оставить для резервного копирования или удалить:
- `database/seed-projects.sql` - SQL скрипт для ручного добавления проектов

**На сервере:** Если не нужны, удалите:
```bash
rm /home/n/nikolr4t/develonik.ru/public_html/database/seed-projects.sql
```

## Файлы, которые НЕ нужно удалять

### Обязательные файлы (НЕ удалять):
- ✅ `api/admin/seed-initial-projects.php` - используется кнопкой в админ-панели
- ✅ `api/admin/get-projects.php` - получение списка проектов
- ✅ `api/admin/save-project.php` - сохранение проектов
- ✅ `api/admin/delete-project.php` - удаление проектов
- ✅ `api/admin/save-site-setting.php` - сохранение фото
- ✅ Все остальные API endpoints

## Быстрая проверка на сервере

Проверьте, какие временные файлы еще есть:

```bash
# Проверка временных скриптов
ls -la /home/n/nikolr4t/develonik.ru/public_html/api/check-permissions.php 2>/dev/null && echo "⚠️ check-permissions.php exists" || echo "✅ check-permissions.php removed"
ls -la /home/n/nikolr4t/develonik.ru/public_html/api/admin/seed-projects.php 2>/dev/null && echo "⚠️ seed-projects.php exists" || echo "✅ seed-projects.php removed"
```

## Рекомендации

1. **Удалите временные скрипты** - они больше не нужны и могут быть уязвимостью
2. **Оставьте SQL скрипты** - они могут пригодиться для резервного копирования
3. **Проверьте права доступа** - убедитесь, что только нужные файлы доступны через веб

---

**После удаления временных файлов проект будет более безопасным!**

