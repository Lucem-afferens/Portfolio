# 🎨 Дизайн блока "Мои проекты" — Техническое задание

## 📋 Общая концепция

Блок проектов должен быть:
- ✅ **Минималистичным** — фокус на контенте, без лишних элементов
- ✅ **Производительным** — оптимизация для Core Web Vitals (LCP < 2.5s, CLS < 0.1)
- ✅ **Доступным** — семантический HTML, ARIA-атрибуты, keyboard navigation
- ✅ **Адаптивным** — mobile-first подход, корректная работа на всех устройствах
- ✅ **Интерактивным** — плавные hover-эффекты, feedback при взаимодействии

---

## 🏗️ Структура HTML

```html
<section id="projects" class="projects" aria-labelledby="projects-title">
  <div class="container">
    <header class="projects__header">
      <h2 id="projects-title" class="projects__title">Мои проекты</h2>
      <p class="projects__subtitle">Лучшие работы, отражающие мой подход</p>
    </header>
    
    <nav class="projects__filters" aria-label="Фильтр проектов">
      <button class="projects__filter active" data-filter="all" aria-pressed="true">
        Все
      </button>
      <button class="projects__filter" data-filter="web" aria-pressed="false">
        Web
      </button>
      <button class="projects__filter" data-filter="ui" aria-pressed="false">
        UI
      </button>
      <button class="projects__filter" data-filter="branding" aria-pressed="false">
        Branding
      </button>
    </nav>
    
    <div class="projects__grid" role="list" aria-label="Список проектов">
      <article class="projects__card" role="listitem" data-category="web">
        <a href="#project-1" class="projects__card-link" aria-label="Открыть проект: Название проекта">
          <div class="projects__card-image-wrapper">
            <img 
              src="/images/project-1.webp"
              srcset="/images/project-1-400.webp 400w, /images/project-1-800.webp 800w"
              sizes="(max-width: 768px) 100vw, (max-width: 1200px) 50vw, 33vw"
              alt="Превью проекта: Название проекта"
              class="projects__card-image"
              loading="lazy"
              width="800"
              height="600"
              decoding="async"
            />
            <div class="projects__card-overlay" aria-hidden="true"></div>
          </div>
          <div class="projects__card-content">
            <h3 class="projects__card-title">Название проекта</h3>
            <p class="projects__card-role">Роль: Front-end Developer</p>
            <div class="projects__card-tags" aria-label="Технологии">
              <span class="projects__tag">React</span>
              <span class="projects__tag">TypeScript</span>
              <span class="projects__tag">SCSS</span>
            </div>
          </div>
        </a>
      </article>
      <!-- Повторить для каждого проекта -->
    </div>
  </div>
</section>
```

---

## 🎨 Цветовая палитра (актуальная)

Используем только primary цвет (#1FA6A0), accent цвет удален из дизайна.

| Элемент | CSS Variable | Light Theme | Dark Theme |
|---------|-------------|-------------|------------|
| Фон секции | `--bg-main` | `#F5F6F4` | `#0E1416` |
| Фон карточки | `--bg-surface` | `#FFFFFF` | `#141C1F` |
| Заголовок | `--text-primary` | `#2F3A4A` | `#E7ECEB` |
| Вторичный текст | `--text-secondary` | `#5C6675` | `#B5C1BE` |
| Акцент (hover, ссылки) | `--color-primary` | `#1FA6A0` | `#1FA6A0` |
| Границы | `--border-color` | `#E1E4DE` | `#243236` |
| Тени | `--shadow-soft` | `0 8px 24px rgba(0,0,0,0.04)` | `0 8px 24px rgba(0,0,0,0.4)` |

---

## 📐 Сетка и адаптивность

### Desktop (≥ 992px)
- **3 колонки** — по одной карточке в ряд
- Gap: 32px между карточками
- Max-width контейнера: 1200px

### Tablet (768px - 991px)
- **2 колонки** — сетка 2×2
- Gap: 24px
- Если нечетное количество — последняя карточка центрируется

### Mobile (< 768px)
- **1 колонка** — вертикальный список
- Gap: 24px
- Карточки на всю ширину контейнера

---

## 🎴 Карточка проекта

### Структура карточки

```
┌─────────────────────────┐
│   [Изображение 16:9]    │
│   (с overlay при hover)  │
├─────────────────────────┤
│   Название проекта      │
│   Роль: Front-end       │
│   [React] [TS] [SCSS]   │
└─────────────────────────┘
```

### Спецификация изображения

- **Aspect ratio**: 16:9 (рекомендуется для единообразия)
- **Формат**: WebP с fallback на JPG
- **Responsive images**: `srcset` с размерами 400w, 800w, 1200w
- **Lazy loading**: `loading="lazy"` для всех изображений ниже fold
- **Размеры**: явно указаны `width` и `height` для предотвращения CLS
- **Object-fit**: `cover` для сохранения пропорций
- **Оптимизация**: сжатие до 80% качества, прогрессивная загрузка

### Overlay при hover

- **Градиент**: `linear-gradient(to top, rgba(0,0,0,0.6) 0%, transparent 100%)`
- **Анимация**: плавное появление (0.3s ease)
- **Назначение**: улучшение читаемости текста на изображении

---

## 🎭 Взаимодействие (Interactions)

### Hover эффекты

```scss
.projects__card {
  // Базовая тень
  box-shadow: var(--shadow-soft);
  transition: transform 0.3s ease, box-shadow 0.3s ease;
  
  &:hover {
    // Подъем карточки
    transform: translateY(-8px);
    // Усиление тени
    box-shadow: 0 12px 32px rgba(0, 0, 0, 0.12);
    
    // Плавное увеличение изображения
    .projects__card-image {
      transform: scale(1.05);
    }
    
    // Появление overlay
    .projects__card-overlay {
      opacity: 1;
    }
    
    // Подъем контента
    .projects__card-content {
      transform: translateY(-4px);
    }
  }
}
```

### Touch взаимодействие (Mobile)

```scss
.projects__card:active {
  transform: translateY(-4px) scale(0.98);
  transition: transform 0.1s ease;
}
```

### Focus состояния (Accessibility)

```scss
.projects__card-link:focus-visible {
  outline: 2px solid var(--color-primary);
  outline-offset: 4px;
  border-radius: 8px;
}
```

---

## 📝 Типографика

### Заголовок секции

```scss
.projects__title {
  font-size: 2.5rem;      // 40px
  font-weight: 700;
  line-height: 1.2;
  color: var(--text-primary);
  margin-bottom: 0.5rem;
  
  @media (max-width: 768px) {
    font-size: 2rem;     // 32px
  }
}
```

### Подзаголовок

```scss
.projects__subtitle {
  font-size: 1rem;       // 16px
  color: var(--text-secondary);
  margin-bottom: 2rem;
}
```

### Название проекта

```scss
.projects__card-title {
  font-size: 1.25rem;     // 20px
  font-weight: 600;
  line-height: 1.3;
  color: var(--text-primary);
  margin-bottom: 0.5rem;
}
```

### Роль в проекте

```scss
.projects__card-role {
  font-size: 0.875rem;    // 14px
  color: var(--text-secondary);
  margin-bottom: 0.75rem;
}
```

### Теги технологий

```scss
.projects__tag {
  display: inline-block;
  font-size: 0.75rem;     // 12px
  padding: 0.25rem 0.75rem;
  background-color: var(--bg-muted);
  color: var(--text-secondary);
  border-radius: 12px;
  margin-right: 0.5rem;
  margin-bottom: 0.5rem;
}
```

---

## 🎯 Фильтры проектов

### Поведение

- **Активный фильтр**: фон `--color-primary`, текст `--bg-surface`, тень при hover
- **Неактивный**: прозрачный фон, граница `--border-color`
- **Hover**: тень `rgba(31, 166, 160, 0.3)`
- **Анимация**: плавное переключение (0.2s ease)

### Accessibility

- `aria-pressed="true/false"` для состояния
- `aria-label` для навигации
- Keyboard navigation (Tab, Enter/Space)

---

## ⚡ Оптимизация производительности

### Core Web Vitals

1. **LCP (Largest Contentful Paint) < 2.5s**
   - Первое изображение: `loading="eager"` (выше fold)
   - Остальные: `loading="lazy"`
   - Использование WebP с fallback
   - Явные размеры изображений

2. **CLS (Cumulative Layout Shift) < 0.1**
   - Фиксированные размеры для всех изображений
   - Резервное место (skeleton) при загрузке
   - Избежание динамического контента без размеров

3. **FID (First Input Delay) < 100ms**
   - Минимальный JavaScript
   - Event delegation для фильтров
   - CSS transitions вместо JS анимаций

### Оптимизация изображений

```javascript
// Рекомендуемая структура данных проекта
{
  id: 1,
  title: "Название проекта",
  role: "Front-end Developer",
  category: "web",
  image: {
    src: "/images/project-1.webp",
    srcset: "/images/project-1-400.webp 400w, /images/project-1-800.webp 800w",
    width: 800,
    height: 600,
    alt: "Превью проекта: Название проекта"
  },
  tags: ["React", "TypeScript", "SCSS"],
  link: "#project-1"
}
```

### Lazy Loading стратегия

- **Intersection Observer** для более точного контроля
- **Placeholder** с низким качеством (LQIP) или цветом фона
- **Progressive enhancement**: нативный `loading="lazy"` как fallback

---

## 🎨 Визуальные улучшения

### Плавные переходы

Все анимации используют `will-change` для оптимизации:

```scss
.projects__card {
  will-change: transform;
  
  &:hover {
    will-change: transform, box-shadow;
  }
}
```

### Skeleton loading

При загрузке показывать placeholder:

```html
<div class="projects__card-skeleton" aria-hidden="true">
  <div class="skeleton-image"></div>
  <div class="skeleton-content">
    <div class="skeleton-line"></div>
    <div class="skeleton-line short"></div>
  </div>
</div>
```

---

## 📱 Mobile оптимизации

### Touch-friendly

- Минимальный размер кликабельной области: 44×44px
- Увеличенные отступы между карточками
- Swipe gestures для навигации (опционально)

### Производительность на мобильных

- Упрощенные hover-эффекты (только для устройств с hover)
- Меньшие размеры изображений для мобильных
- Оптимизация анимаций (reduced motion support)

---

## ♿ Доступность (A11y)

### ARIA атрибуты

- `role="list"` и `role="listitem"` для сетки
- `aria-label` для фильтров и карточек
- `aria-pressed` для активного фильтра
- `aria-hidden="true"` для декоративных элементов

### Keyboard navigation

- Tab для навигации между карточками
- Enter/Space для открытия проекта
- Стрелки для фильтров (опционально)

### Screen readers

- Семантический HTML (`<article>`, `<nav>`, `<header>`)
- Описательные alt-тексты для изображений
- Скрытие декоративных элементов от скринридеров

---

## 🚀 Итоговая спецификация

### Компонент: Projects

**Структура:**
- Header (заголовок + подзаголовок)
- Navigation (фильтры)
- Grid (сетка карточек)

**Карточка проекта:**
- Изображение (16:9, WebP, lazy load)
- Overlay (при hover)
- Контент (название, роль, теги)
- Ссылка (весь card кликабелен)

**Адаптивность:**
- Desktop: 3 колонки
- Tablet: 2 колонки
- Mobile: 1 колонка

**Цвета:**
- Только primary цвет (#1FA6A0)
- Без accent цвета
- Тени при hover

**Производительность:**
- Lazy loading изображений
- Явные размеры (CLS prevention)
- Минимальный JavaScript
- CSS transitions

**Доступность:**
- Семантический HTML
- ARIA атрибуты
- Keyboard navigation
- Screen reader support

---

## 📌 Ключевые принципы

1. **Минимализм** — убрать все лишнее, фокус на контенте
2. **Производительность** — оптимизация для Core Web Vitals
3. **Доступность** — работа для всех пользователей
4. **Единообразие** — один primary цвет, согласованные отступы
5. **Простота** — избегать сложных анимаций и эффектов

---

## ✅ Чеклист реализации

- [ ] Семантическая HTML структура
- [ ] CSS Grid для адаптивной сетки
- [ ] WebP изображения с fallback
- [ ] Lazy loading для всех изображений
- [ ] Явные размеры изображений (width/height)
- [ ] ARIA атрибуты для доступности
- [ ] Keyboard navigation
- [ ] Hover эффекты (только для hover-устройств)
- [ ] Touch feedback для мобильных
- [ ] Оптимизация для Core Web Vitals
- [ ] Тестирование на разных устройствах
- [ ] Проверка доступности (a11y)
