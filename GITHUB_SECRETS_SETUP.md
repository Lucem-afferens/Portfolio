# 🔐 Настройка GitHub Secrets для автоматического деплоя

## 📋 Необходимые секреты

В настройках вашего GitHub репозитория (`Settings` → `Secrets and variables` → `Actions`) создайте следующие секреты:

### 1. FTP_SERVER
**Описание:** Адрес FTP сервера Beget  
**Пример:** `nikolr4t.beget.tech` или IP адрес  
**Где найти:** В панели управления Beget → FTP доступы

### 2. FTP_USERNAME
**Описание:** Имя пользователя FTP  
**Пример:** `nikolr4t`  
**Где найти:** В панели управления Beget → FTP доступы

### 3. FTP_PASSWORD
**Описание:** Пароль от FTP  
**Где найти:** В панели управления Beget → FTP доступы (или создайте новый пароль)

### 4. FTP_SERVER_DIR
**Описание:** Директория на сервере для загрузки файлов  
**Пример:** `/public_html/` или `/public_html/portfolio/`  
**По умолчанию:** `/public_html/` (если не указано)

### 5. FTP_BOT_TOKEN
**Описание:** Токен Telegram бота (для уведомлений о новых отзывах)  
**Где получить:**
1. Откройте Telegram → найдите **@BotFather**
2. Отправьте `/newbot` и следуйте инструкциям
3. Скопируйте токен (выглядит как: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)
4. Вставьте в секрет `FTP_BOT_TOKEN`

### 6. FTP_CHAT_ID
**Описание:** Ваш Chat ID в Telegram (для получения уведомлений)  
**Где получить:**
1. Откройте Telegram → найдите **@userinfobot**
2. Отправьте `/start`
3. Скопируйте ваш Chat ID (число, например: `123456789`)
4. Вставьте в секрет `FTP_CHAT_ID`

### 7. FTP_ADMIN_PASSWORD
**Описание:** Пароль для входа в админ-панель модерации отзывов  
**Требования:**
- Минимум 12 символов
- Буквы, цифры, символы
- Безопасный пароль

**Важно:** Этот пароль будет автоматически захеширован при деплое. Запомните его - он нужен для входа в `/admin.html`

## 🔄 Как это работает

1. При каждом push в ветку `main` запускается workflow
2. Проект собирается (npm run build)
3. PHP файлы копируются в dist/
4. **Автоматически создаются config.php файлы из секретов:**
   - `dist/database/config.php` - с данными БД
   - `dist/api/config.php` - с Telegram токенами и паролем админа
5. Все файлы загружаются на FTP сервер

## ✅ Проверка после настройки

После добавления всех секретов:

1. Сделайте любой commit и push в `main`
2. Перейдите в `Actions` на GitHub
3. Убедитесь, что workflow запустился и завершился успешно
4. Проверьте сайт: `https://develonik.ru`
5. Проверьте админ-панель: `https://develonik.ru/admin.html`
6. Проверьте форму отзывов: `https://develonik.ru/testimonial-form.html`

## 🔒 Безопасность

- ✅ Все секреты хранятся в GitHub Secrets (зашифрованы)
- ✅ config.php файлы создаются только при деплое
- ✅ Секреты никогда не попадают в git репозиторий
- ✅ Пароль админа автоматически хешируется

## 🐛 Решение проблем

### Workflow не запускается
- Проверьте, что секреты созданы правильно
- Убедитесь, что push в ветку `main`

### Ошибка подключения к FTP
- Проверьте правильность `FTP_SERVER`, `FTP_USERNAME`, `FTP_PASSWORD`
- Убедитесь, что FTP доступен с внешних IP

### Config файлы не создаются
- Проверьте логи workflow в GitHub Actions
- Убедитесь, что все секреты заполнены

### Пароль админа не работает
- Убедитесь, что используете тот же пароль, что в секрете `FTP_ADMIN_PASSWORD`
- Проверьте, что workflow выполнился успешно

---

**Готово!** После настройки всех секретов деплой будет происходить автоматически при каждом push.

