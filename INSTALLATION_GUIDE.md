# 📋 Пошаговая инструкция по установке системы отзывов

## ✅ Шаг 1: Проверка базы данных

Ваша база данных уже создана:
- **Имя БД:** `nikolr4t_portfol`
- **Пароль:** `4I3%8gC2QF86`
- **Username:** `nikolr4t` (предположительно, если отличается - укажите)

## 📝 Шаг 2: Импорт структуры базы данных

### Вариант A: Через phpMyAdmin (рекомендуется)

1. Войдите в **phpMyAdmin** на вашем хостинге
2. Выберите базу данных `nikolr4t_portfol` в левом меню
3. Перейдите на вкладку **"SQL"**
4. Откройте файл `database/install.sql` из проекта
5. Скопируйте весь SQL код из файла
6. Вставьте в поле ввода SQL в phpMyAdmin
7. Нажмите **"Вперед"** (Execute)

### Вариант B: Через командную строку (если есть доступ)

```bash
mysql -u nikolr4t -p nikolr4t_portfol < database/install.sql
# Введите пароль: 4I3%8gC2QF86
```

### Проверка успешного импорта

После импорта в phpMyAdmin вы должны увидеть:
- Таблицу `testimonials` с колонками: id, name, position, company, message, photo, status, rejection_reason, created_at, moderated_at, ip_address
- Таблицу `admins` (для будущего использования)

## ⚙️ Шаг 3: Настройка конфигурации

### 3.1. Конфигурация базы данных

Файл `database/config.php` уже создан с вашими данными. Проверьте:

```php
'dbname' => 'nikolr4t_portfol',
'username' => 'nikolr4t',  // Если отличается, исправьте
'password' => '4I3%8gC2QF86',
```

**Если username отличается от `nikolr4t`**, откройте файл и исправьте.

### 3.2. Настройка Telegram бота

1. **Создайте бота:**
   - Откройте Telegram
   - Найдите [@BotFather](https://t.me/BotFather)
   - Отправьте команду `/newbot`
   - Следуйте инструкциям (придумайте имя и username для бота)
   - **Скопируйте токен** (выглядит как `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

2. **Узнайте свой Chat ID:**
   - Найдите [@userinfobot](https://t.me/userinfobot) в Telegram
   - Отправьте команду `/start`
   - Бот вернет ваш Chat ID (число, например `123456789`)

3. **Настройте API:**
   - Откройте файл `api/config.php`
   - Найдите строки:
     ```php
     define('TELEGRAM_BOT_TOKEN', 'YOUR_BOT_TOKEN');
     define('TELEGRAM_CHAT_ID', 'YOUR_CHAT_ID');
     ```
   - Замените на ваши значения:
     ```php
     define('TELEGRAM_BOT_TOKEN', 'ваш_токен_от_BotFather');
     define('TELEGRAM_CHAT_ID', 'ваш_chat_id');
     ```

### 3.3. Настройка пароля админа

1. Откройте файл `api/config.php`
2. Найдите строку:
   ```php
   define('ADMIN_PASSWORD_HASH', password_hash('your_secure_password', PASSWORD_DEFAULT));
   ```
3. Замените `'your_secure_password'` на ваш пароль (например, `'MySecurePass123!'`)
4. Сохраните файл

**Важно:** Запомните этот пароль - он нужен для входа в админ-панель!

### 3.4. Настройка Email (опционально)

Email уже настроен на `nikwebdev.2025@gmail.com`. Если нужно изменить:
- Откройте `api/config.php`
- Найдите `define('ADMIN_EMAIL', 'nikwebdev.2025@gmail.com');`
- Измените на нужный email

## 🧪 Шаг 4: Тестирование

### 4.1. Проверка подключения к БД

Создайте файл `api/test-db.php`:

```php
<?php
require_once __DIR__ . '/db.php';
echo json_encode(['success' => true, 'message' => 'Database connected!']);
```

Откройте в браузере: `https://develonik.ru/api/test-db.php`

Должно появиться: `{"success":true,"message":"Database connected!"}`

Если ошибка - проверьте данные в `database/config.php`

### 4.2. Тест отправки отзыва

1. Откройте: `https://develonik.ru/testimonial-form.html`
2. Заполните форму
3. Отправьте отзыв
4. Проверьте:
   - Появилось ли сообщение "Отзыв отправлен на модерацию"
   - Пришло ли уведомление в Telegram
   - Пришло ли письмо на email

### 4.3. Тест админ-панели

1. Откройте: `https://develonik.ru/admin.html`
2. Введите пароль, который указали в `api/config.php`
3. Должна открыться панель модерации
4. Проверьте вкладку "На модерации" - должен быть ваш тестовый отзыв

## 🔒 Шаг 5: Безопасность

### 5.1. Проверка прав доступа

Убедитесь, что файл `database/config.php` защищен:
```bash
chmod 600 database/config.php
```

### 5.2. Проверка .gitignore

Убедитесь, что `database/config.php` в `.gitignore` (уже добавлен)

### 5.3. HTTPS

Убедитесь, что сайт работает по HTTPS (для безопасности паролей)

## 📍 Шаг 6: Интеграция с сайтом

### 6.1. Добавление ссылки на форму отзывов

Добавьте в навигацию или в секцию Contact ссылку:
```html
<a href="/testimonial-form.html">Оставить отзыв</a>
```

### 6.2. Проверка отображения отзывов

После одобрения отзыва через админ-панель, он должен автоматически появиться в секции Testimonials на главной странице.

## 🐛 Решение проблем

### Ошибка подключения к БД

**Проблема:** `Database connection failed`

**Решение:**
1. Проверьте правильность username в `database/config.php`
2. Проверьте, что БД существует
3. Проверьте права доступа пользователя к БД

### Telegram уведомления не приходят

**Проблема:** Отзывы сохраняются, но уведомления не приходят

**Решение:**
1. Проверьте правильность токена бота
2. Проверьте правильность Chat ID
3. Убедитесь, что бот запущен (отправьте `/start` боту)
4. Проверьте, что на сервере доступен `curl` (для отправки запросов)

### Email не отправляется

**Проблема:** Email уведомления не приходят

**Решение:**
1. Проверьте настройки `mail()` в PHP
2. На shared хостингах может потребоваться настройка SMTP
3. Проверьте папку "Спам" в почте

### Админ-панель не открывается

**Проблема:** Не могу войти в админ-панель

**Решение:**
1. Проверьте правильность пароля в `api/config.php`
2. Убедитесь, что используете тот же пароль при входе
3. Очистите cookies браузера
4. Проверьте консоль браузера на ошибки (F12)

## ✅ Чеклист готовности

- [ ] База данных создана и структура импортирована
- [ ] `database/config.php` настроен с правильными данными
- [ ] Telegram бот создан, токен и Chat ID указаны в `api/config.php`
- [ ] Пароль админа установлен в `api/config.php`
- [ ] Тест подключения к БД успешен
- [ ] Тест отправки отзыва работает
- [ ] Уведомления приходят в Telegram
- [ ] Админ-панель открывается и работает
- [ ] Отзывы отображаются на сайте после одобрения

## 📞 Дополнительная помощь

Если возникли проблемы:
1. Проверьте логи ошибок PHP на сервере
2. Проверьте консоль браузера (F12) на ошибки JavaScript
3. Убедитесь, что все файлы загружены на сервер
4. Проверьте права доступа к файлам (644 для PHP, 755 для директорий)

---

**Готово!** Система отзывов должна работать. Если что-то не работает - проверьте каждый шаг по порядку.


