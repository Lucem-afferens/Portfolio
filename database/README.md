# ⚠️ ВАЖНО: Файл config.php содержит секретные данные

## Не коммитьте config.php в git!

Файл уже добавлен в .gitignore, но убедитесь, что он не попал в репозиторий.

## Текущая конфигурация

- **База данных:** nikolr4t_portfol
- **Username:** nikolr4t (если отличается - исправьте в config.php)
- **Пароль:** настроен в config.php

## Следующие шаги

1. Импортируйте `install.sql` в вашу БД
2. Настройте Telegram в `../api/config.php`
3. Установите пароль админа в `../api/config.php`

См. `../INSTALLATION_GUIDE.md` для подробных инструкций.
