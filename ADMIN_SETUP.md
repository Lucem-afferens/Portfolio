# 🔐 Инструкция по настройке администратора

## Проблема

Если вы использовали SQL запрос с placeholder `$2y$10$...`, то пароль не будет работать, так как это не реальный хеш, а просто пример.

## ✅ Правильная настройка

### Шаг 1: Сгенерируйте хеш пароля

Выберите один из способов:

#### Способ А: PHP скрипт (рекомендуется)

1. Откройте в браузере: `https://develonik.ru/database/generate-password-hash.php`
2. Введите желаемый пароль (минимум 8 символов, рекомендуется сложный)
3. Нажмите "Сгенерировать хеш"
4. Скопируйте сгенерированный хеш

#### Способ Б: Командная строка PHP

```bash
php -r "echo password_hash('ваш_пароль', PASSWORD_DEFAULT);"
```

#### Способ В: Онлайн генератор

Используйте: https://bcrypt-generator.com/

### Шаг 2: Создайте администратора в БД

Выполните SQL запрос в phpMyAdmin или через командную строку:

```sql
-- Если админа еще нет
INSERT INTO admins (username, password_hash) 
VALUES ('admin', 'ВАШ_СГЕНЕРИРОВАННЫЙ_ХЕШ_ЗДЕСЬ');

-- Или если админ уже существует (обновить пароль)
UPDATE admins 
SET password_hash = 'ВАШ_СГЕНЕРИРОВАННЫЙ_ХЕШ_ЗДЕСЬ' 
WHERE username = 'admin';
```

**Пример:**
```sql
INSERT INTO admins (username, password_hash) 
VALUES ('admin', '$2y$10$N9qo8uLOickgx2ZMRZoMyeIjZAgcfl7p92ldGxad68LJZdL17lhWy');
```

### Шаг 3: Войдите в админ-панель

1. Откройте: **https://develonik.ru/pages/admin.html**
2. Введите:
   - **Логин:** `admin` (или тот, который вы указали в SQL)
   - **Пароль:** тот пароль, который вы использовали для генерации хеша
3. Нажмите "Войти"

## 🔍 Проверка

После создания администратора, проверьте что он создан:

```sql
SELECT id, username, created_at FROM admins;
```

Вы должны увидеть запись с вашим логином.

## ⚠️ Важные замечания

1. **НЕ используйте** placeholder `$2y$10$...` - это не работает!
2. **НЕ храните** пароли в открытом виде
3. **Используйте** сложные пароли (минимум 8 символов, буквы, цифры, символы)
4. **Храните** пароль в безопасном месте (менеджер паролей)

## 🛠️ Если что-то не работает

1. Проверьте, что таблица `admins` существует:
   ```sql
   SHOW TABLES LIKE 'admins';
   ```

2. Проверьте структуру таблицы:
   ```sql
   DESCRIBE admins;
   ```

3. Проверьте, что хеш правильный (должен начинаться с `$2y$10$` и быть длинным)

4. Очистите кэш браузера и попробуйте снова

5. Проверьте логи ошибок PHP на сервере

## 📝 Изменение пароля через админ-панель

После входа в админ-панель вы можете изменить логин и пароль:
1. Перейдите на вкладку **"Настройки"**
2. Введите текущий пароль
3. Введите новый логин и/или пароль
4. Нажмите "Сохранить изменения"

