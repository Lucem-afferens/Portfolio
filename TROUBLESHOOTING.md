# 🔧 Решение проблем

## Проблема 1: `/admin.html` возвращает 404 Not Found

**Причина:** HTML файлы страниц собираются Vite в `dist/pages/`, но должны быть доступны в корне.

**Решение:**
- Workflow автоматически копирует файлы из `dist/pages/` в корень `dist/` при деплое
- После следующего деплоя файлы будут доступны по правильным путям

**Проверка:**
```bash
# После деплоя проверьте на сервере:
ls -la /public_html/admin.html
ls -la /public_html/testimonial-form.html
```

---

## Проблема 2: `/api/test-db.php` возвращает ошибку подключения к БД

**Возможные причины:**

### 2.1. Файл config.php не создан

**Проверка:**
- Убедитесь, что GitHub Secrets настроены
- Проверьте логи workflow в GitHub Actions
- Убедитесь, что шаг "Create database config from secrets" выполнился успешно

**Решение:**
- Проверьте наличие файла на сервере: `/public_html/database/config.php`
- Если файла нет - проверьте логи деплоя

### 2.2. Неправильные данные БД

**Проверка:**
Откройте `/public_html/database/config.php` и проверьте:
```php
return [
    'host' => 'localhost',
    'dbname' => 'nikolr4t_portfol',
    'username' => 'nikolr4t',  // Проверьте правильность
    'password' => '4I3%8gC2QF86',
    'charset' => 'utf8mb4',
];
```

**Решение:**
- Если username отличается - обновите workflow или создайте файл вручную
- Убедитесь, что пароль правильный

### 2.3. База данных не создана или таблицы не импортированы

**Проверка:**
- Войдите в phpMyAdmin
- Проверьте наличие БД `nikolr4t_portfol`
- Проверьте наличие таблицы `testimonials`

**Решение:**
1. Импортируйте `database/install.sql` через phpMyAdmin
2. Убедитесь, что таблицы созданы

### 2.4. Проблемы с правами доступа

**Проверка:**
```bash
# На сервере проверьте права:
ls -la /public_html/database/config.php
ls -la /public_html/api/config.php
```

**Решение:**
```bash
# Установите правильные права:
chmod 644 /public_html/database/config.php
chmod 644 /public_html/api/config.php
```

---

## Проблема 3: Улучшенная диагностика ошибок

Обновленный `test-db.php` теперь показывает:
- Существует ли файл config.php
- Существует ли database/config.php
- Детальное сообщение об ошибке подключения
- Тип исключения

**Использование:**
Откройте `https://develonik.ru/api/test-db.php` и проверьте JSON ответ для диагностики.

---

## Быстрая проверка после деплоя

1. **Проверьте файлы:**
   ```bash
   # На сервере через FTP или SSH:
   ls -la /public_html/admin.html
   ls -la /public_html/testimonial-form.html
   ls -la /public_html/api/test-db.php
   ls -la /public_html/database/config.php
   ls -la /public_html/api/config.php
   ```

2. **Проверьте URL:**
   - `https://develonik.ru/` - главная страница
   - `https://develonik.ru/admin.html` - админ-панель
   - `https://develonik.ru/testimonial-form.html` - форма отзывов
   - `https://develonik.ru/api/test-db.php` - тест БД

3. **Проверьте логи:**
   - GitHub Actions → последний workflow run
   - Логи PHP на сервере (если доступны)

---

## Если ничего не помогает

1. **Проверьте GitHub Secrets:**
   - Все секреты должны быть заполнены
   - Особенно `FTP_SERVER`, `FTP_USERNAME`, `FTP_PASSWORD`

2. **Проверьте деплой:**
   - Workflow должен завершиться успешно
   - Все файлы должны быть загружены

3. **Создайте файлы вручную:**
   - Если автоматическое создание не работает, создайте `config.php` файлы вручную на сервере
   - Используйте данные из `database/config.example.php` и `api/config.php` как шаблон

4. **Проверьте БД:**
   - Убедитесь, что БД создана
   - Импортируйте `install.sql` вручную через phpMyAdmin

